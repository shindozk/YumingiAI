const mongoose = require("mongoose");

module.exports = {
    async execute(client) {
        /*try {
            mongoose.connect(client.config.MongodbConnect);
            console.log("Mongodb connected!")
        } catch (error) {
            console.log(error);
        }*/
    }
}